CREATE table de.user (
    userID      int,
    username    VARCHAR(40),
    password    VARCHAR(40)
);

CREATE table de.laptop (
    part        VARCHAR(30),
    price       int
);

CREATE table de.desktop (
    partname    text,
    shortname   VARCHAR(30),
    price       int
);